# BlogTown

A tiny local blog starter built with the Python standard library.

## Run

```bash
python main.py
```

Open `http://127.0.0.1:8000` and publish a post from the form.

## Test

```bash
python -m unittest discover
```
