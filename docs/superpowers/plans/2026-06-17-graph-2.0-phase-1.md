# 图谱 2.0 Phase 1（数据底座 + A 主题星系）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 `/graph` 加上"主题星系"——对 embedding 聚类、用 LLM 起中文星系名、节点按星系上色、漂浮星系标签 + 图例,并把回放/漫游/路径的"动词药丸"壳先放上(Phase 2 接逻辑)。

**Architecture:** 沿用既有"纯逻辑模块 + 一个薄 DB 读写"的拆分(镜像 `graph.py`/`collide.py`)。聚类在 `sync` 时计算并存入派生 DB(`items.cluster` 列 + `clusters` 表),全部可空、可由 `sync --rebuild` 重建,文件仍是唯一真相源。命名走现有 `alias.py` 的批量 LLM 内核,失败即降级不崩溃。前端在现有 `graph.html` 的 Three.js 星球上叠加按簇上色 + 星系标签 + 图例。

**Tech Stack:** Python 3 / FastAPI / Jinja2 / SQLite(+FTS5)/ numpy(已在,不引入 sklearn)/ Three.js r128(CDN,已在)/ pytest。

**关键决策(已在 spec 确认 + 计划阶段细化):**
- 聚类 k = `clamp(round(√(n/3)), 3, 8)`;`n < 3` → 1。
- 聚类用 numpy k-means,固定 seed → 确定性;在 L2 单位化向量上跑(等价余弦聚类)。
- 颜色:固定 8 色调色板按 `cluster id` 取,存入 `clusters` 表,后端下发。
- **命名只在簇成员"签名"变化时调用 LLM**:`clusters` 表存每簇的成员签名 + 名字;重算后若某簇的精确成员集合与旧表中某签名一致,则沿用旧名、不再调 LLM。这样改笔记(会重嵌入、重聚类)只要没改变聚类边界,就不产生命名 LLM 调用——精确兑现 spec 的"改笔记不重命名"意图。
- 触发:聚类重算与边重算同条件(`embedded_ids or stale`)。

---

## File Structure

**新增**
- `src/aishelf/db/cluster.py` — 纯逻辑(`choose_k`/`kmeans`/`representatives`/`color_for`/`PALETTE`)+ 一个薄 DB 写入(`recompute_clusters`)。
- `tests/unit/test_db_cluster.py` — 上述的单元测试。

**修改**
- `src/aishelf/db/schema.py` — `clusters` 表 + `items.cluster` 列。
- `src/aishelf/db/__main__.py` — `--rebuild` 的 DROP 列表加 `clusters`。
- `src/aishelf/alias.py` — `generate_cluster_names`。
- `src/aishelf/db/sync.py` — 在边重算后触发聚类 + 命名。
- `src/aishelf/db/graph.py` — `load_graph` 输出 `cluster`/`collected_at`/`hook` + 顶层 `clusters`。
- `src/aishelf/site/templates/graph.html` — 按簇上色 / 星系标签 / 图例 / 动词药壳。
- `tests/unit/test_db_sync.py`、`tests/unit/test_db_graph.py`、`tests/unit/test_alias.py` — 追加测试。
- `CLAUDE.md` — 同步 db/graph 描述与部署说明。

---

## Task 1: Schema — `clusters` 表 + `items.cluster` 列

**Files:**
- Modify: `src/aishelf/db/schema.py`
- Modify: `src/aishelf/db/__main__.py`
- Test: `tests/unit/test_db_graph.py`

- [ ] **Step 1: 写失败测试**(追加到 `tests/unit/test_db_graph.py` 末尾)

```python
def test_clusters_table_created(tmp_path):
    con = schema.connect(tmp_path / "atlas.db")
    schema.init_db(con)
    names = {r["name"] for r in con.execute("PRAGMA table_info(clusters)")}
    con.close()
    assert names == {"id", "name", "color", "signature"}


def test_items_table_has_cluster_column(tmp_path):
    con = schema.connect(tmp_path / "atlas.db")
    schema.init_db(con)
    cols = {r["name"] for r in con.execute("PRAGMA table_info(items)")}
    con.close()
    assert "cluster" in cols
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/unit/test_db_graph.py::test_clusters_table_created tests/unit/test_db_graph.py::test_items_table_has_cluster_column -v`
Expected: FAIL（`clusters` 表不存在 / `cluster` 列不存在）。

- [ ] **Step 3: 改 `schema.py`**

在 `items` 表的 `hook TEXT,` 行之后加一列 `cluster INTEGER,`(置于 `content_hash` 之前）：

```python
  alias             TEXT,
  hook              TEXT,
  cluster           INTEGER,
  content_hash  TEXT NOT NULL,
```

在 `SCHEMA_SQL` 末尾、`edges` 表 `CREATE TABLE` 之后追加 `clusters` 表：

```python
CREATE TABLE IF NOT EXISTS clusters (
  id        INTEGER PRIMARY KEY,
  name      TEXT,
  color     TEXT,
  signature TEXT
);
```

- [ ] **Step 4: 改 `__main__.py` 的 rebuild DROP**

把 DROP 脚本改成同时丢弃 `clusters`：

```python
            con.executescript(
                "DROP TABLE IF EXISTS items; "
                "DROP TABLE IF EXISTS items_fts; "
                "DROP TABLE IF EXISTS edges; "
                "DROP TABLE IF EXISTS clusters;"
            )
```

- [ ] **Step 5: 跑测试确认通过**

Run: `pytest tests/unit/test_db_graph.py -v`
Expected: PASS（含既有用例）。

- [ ] **Step 6: 提交**

```bash
git add src/aishelf/db/schema.py src/aishelf/db/__main__.py tests/unit/test_db_graph.py
git commit -m "feat(db): clusters 表 + items.cluster 列（图谱主题星系底座）"
```

---

## Task 2: `cluster.py` 纯辅助 — `choose_k` + 调色板

**Files:**
- Create: `src/aishelf/db/cluster.py`
- Test: `tests/unit/test_db_cluster.py`

- [ ] **Step 1: 写失败测试**(新建 `tests/unit/test_db_cluster.py`)

```python
from aishelf.db import cluster


def test_choose_k_small_corpus_is_single_cluster():
    assert cluster.choose_k(0) == 1
    assert cluster.choose_k(2) == 1


def test_choose_k_floor_and_ceiling():
    assert cluster.choose_k(3) == 3          # round(sqrt(1)) = 1 -> floored to 3
    assert cluster.choose_k(100) == 6        # round(sqrt(33.3)) = 6
    assert cluster.choose_k(1000) == 8       # round(sqrt(333)) = 18 -> capped at 8


def test_color_for_cycles_palette():
    assert cluster.color_for(0) == cluster.PALETTE[0]
    assert cluster.color_for(len(cluster.PALETTE)) == cluster.PALETTE[0]
    assert len({cluster.color_for(i) for i in range(len(cluster.PALETTE))}) == len(cluster.PALETTE)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/unit/test_db_cluster.py -v`
Expected: FAIL（`No module named aishelf.db.cluster`）。

- [ ] **Step 3: 创建 `cluster.py`(本任务部分)**

```python
"""Semantic clustering over stored embeddings (主题星系).

Pure helpers (choose_k / kmeans / representatives / color_for) plus one thin DB
writer (recompute_clusters), mirroring the graph.py / collide.py split. numpy
k-means on L2-normalized vectors (cosine clustering); no sklearn. Clusters are a
rebuildable derived view written at sync time.
"""

from __future__ import annotations

import hashlib
import math

import numpy as np

from aishelf.db import graph

CLUSTER_SEED = 1234

# Fixed palette; cluster colour is PALETTE[id % len] so re-syncs stay stable.
PALETTE = [
    "#ff9a3c", "#4cc2ff", "#38e1c0", "#c08cff",
    "#ff6b9d", "#ffd166", "#7ee787", "#8ab4ff",
]


def choose_k(n: int) -> int:
    """How many galaxies for n embedded items: clamp(round(sqrt(n/3)), 3, 8),
    degrading to a single cluster below 3 items."""
    if n < 3:
        return 1
    return max(3, min(8, round(math.sqrt(n / 3))))


def color_for(cluster_id: int) -> str:
    return PALETTE[cluster_id % len(PALETTE)]
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest tests/unit/test_db_cluster.py -v`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add src/aishelf/db/cluster.py tests/unit/test_db_cluster.py
git commit -m "feat(db): cluster.choose_k + 调色板"
```

---

## Task 3: `cluster.kmeans`

**Files:**
- Modify: `src/aishelf/db/cluster.py`
- Test: `tests/unit/test_db_cluster.py`

- [ ] **Step 1: 写失败测试**(追加)

```python
import numpy as np


def test_kmeans_single_cluster_labels_all_zero():
    mat = np.array([[1.0, 0.0], [0.0, 1.0], [0.5, 0.5]], dtype=np.float32)
    labels = cluster.kmeans(mat, 1, seed=1)
    assert list(labels) == [0, 0, 0]


def test_kmeans_separates_two_groups():
    mat = np.array([[1.0, 0.0], [0.99, 0.1], [-1.0, 0.0], [-0.99, 0.1]], dtype=np.float32)
    labels = cluster.kmeans(mat, 2, seed=7)
    assert labels[0] == labels[1]          # right-pointing pair together
    assert labels[2] == labels[3]          # left-pointing pair together
    assert labels[0] != labels[2]          # the two pairs are different clusters


def test_kmeans_is_deterministic():
    mat = np.array([[1.0, 0.0], [0.2, 0.9], [-1.0, 0.1], [0.1, -1.0]], dtype=np.float32)
    a = cluster.kmeans(mat, 2, seed=42)
    b = cluster.kmeans(mat, 2, seed=42)
    assert list(a) == list(b)


def test_kmeans_clamps_k_to_n():
    mat = np.array([[1.0, 0.0], [0.0, 1.0]], dtype=np.float32)
    labels = cluster.kmeans(mat, 5, seed=1)   # k > n
    assert len(labels) == 2 and set(int(x) for x in labels) <= {0, 1}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/unit/test_db_cluster.py -k kmeans -v`
Expected: FAIL（`module 'aishelf.db.cluster' has no attribute 'kmeans'`）。

- [ ] **Step 3: 实现 `kmeans`(+ `_normalize`)**(追加到 `cluster.py`,放在 `color_for` 之后)

```python
def _normalize(mat: np.ndarray) -> np.ndarray:
    norms = np.linalg.norm(mat, axis=1, keepdims=True)
    return mat / (norms + 1e-12)


def kmeans(matrix, k: int, *, seed: int, iters: int = 50):
    """Lloyd's k-means on L2-normalized rows (== cosine clustering). Deterministic
    for a given seed. Returns one int label per row. k is clamped to [1, n]."""
    mat = np.asarray(matrix, dtype=np.float32)
    n = mat.shape[0]
    if n == 0:
        return np.empty((0,), dtype=int)
    k = max(1, min(k, n))
    if k == 1:
        return np.zeros(n, dtype=int)
    x = _normalize(mat)
    rng = np.random.default_rng(seed)
    centroids = x[rng.choice(n, size=k, replace=False)].copy()
    labels = np.full(n, -1, dtype=int)
    for _ in range(iters):
        new_labels = np.argmax(x @ centroids.T, axis=1)   # nearest centroid = max dot on unit vectors
        if np.array_equal(new_labels, labels):
            break
        labels = new_labels
        for c in range(k):
            members = x[labels == c]
            if len(members):
                v = members.mean(axis=0)
                nv = float(np.linalg.norm(v))
                centroids[c] = v / nv if nv > 1e-12 else x[rng.integers(n)]
            else:
                centroids[c] = x[rng.integers(n)]          # reseed an emptied cluster
    return labels
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest tests/unit/test_db_cluster.py -k kmeans -v`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add src/aishelf/db/cluster.py tests/unit/test_db_cluster.py
git commit -m "feat(db): cluster.kmeans（numpy 余弦聚类，确定性）"
```

---

## Task 4: `cluster.representatives`

**Files:**
- Modify: `src/aishelf/db/cluster.py`
- Test: `tests/unit/test_db_cluster.py`

- [ ] **Step 1: 写失败测试**(追加)

```python
def test_representatives_groups_by_label_nearest_first():
    ids = ["a", "b", "c"]
    mat = np.array([[1.0, 0.0], [0.98, 0.2], [0.0, 1.0]], dtype=np.float32)
    labels = np.array([0, 0, 1])
    out = cluster.representatives(labels, mat, ids)
    assert set(out) == {0, 1}
    assert set(out[0]) == {"a", "b"}
    assert out[1] == ["c"]


def test_representatives_caps_to_m():
    ids = [f"i{n}" for n in range(10)]
    mat = np.tile(np.array([1.0, 0.0], dtype=np.float32), (10, 1))
    labels = np.zeros(10, dtype=int)
    out = cluster.representatives(labels, mat, ids, m=3)
    assert len(out[0]) == 3
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/unit/test_db_cluster.py -k representatives -v`
Expected: FAIL。

- [ ] **Step 3: 实现 `representatives`**(追加到 `cluster.py`)

```python
def representatives(labels, matrix, ids, *, m: int = 6) -> dict[int, list[str]]:
    """{cluster_id: [ids]} — up to m ids per cluster, nearest the cluster centroid
    first (cosine on L2-normalized vectors)."""
    x = _normalize(np.asarray(matrix, dtype=np.float32))
    labels = np.asarray(labels)
    out: dict[int, list[str]] = {}
    for c in sorted({int(v) for v in labels}):
        idx = np.where(labels == c)[0]
        members = x[idx]
        centroid = members.mean(axis=0)
        nc = float(np.linalg.norm(centroid))
        if nc > 1e-12:
            centroid = centroid / nc
        order = idx[np.argsort(-(members @ centroid))][:m]
        out[c] = [ids[i] for i in order]
    return out
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest tests/unit/test_db_cluster.py -k representatives -v`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add src/aishelf/db/cluster.py tests/unit/test_db_cluster.py
git commit -m "feat(db): cluster.representatives（每簇代表 id）"
```

---

## Task 5: `cluster.recompute_clusters`（薄 DB 写入 + 签名沿用名字）

**Files:**
- Modify: `src/aishelf/db/cluster.py`
- Test: `tests/unit/test_db_cluster.py`

- [ ] **Step 1: 写失败测试**(追加;含一个本地播种助手)

```python
from aishelf.db import schema, vector


def _seed(con, rid, vec, title=None):
    con.execute(
        "INSERT INTO items (id, type, title, author, platform, source_url, "
        "published_at, collected_at, summary, keywords, embedding, "
        "embedding_model, embedding_dim, content_hash, synced_at) "
        "VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)",
        (rid, "blog", title or f"标题{rid}", "作者", "p", f"https://x/{rid}",
         "2024-01-01", "2024-01-02", "s", "[]", vector.pack(vec), "m",
         len(vec), "h", "2024-01-02"),
    )


def test_recompute_assigns_clusters_and_colors(tmp_path):
    con = schema.connect(tmp_path / "atlas.db")
    schema.init_db(con)
    for rid, vec in [("a", [1.0, 0.0]), ("b", [0.98, 0.1]),
                     ("c", [-1.0, 0.0]), ("d", [-0.98, 0.1])]:
        _seed(con, rid, vec)
    need = cluster.recompute_clusters(con, seed=7)
    assert all(r["cluster"] is not None
               for r in con.execute("SELECT cluster FROM items"))
    rows = list(con.execute("SELECT id, color, signature FROM clusters"))
    assert rows and all(r["color"] in cluster.PALETTE for r in rows)
    assert need                                   # fresh clusters need naming
    assert all(isinstance(t, list) for t in need.values())
    con.close()


def test_recompute_no_embeddings_clears(tmp_path):
    con = schema.connect(tmp_path / "atlas.db")
    schema.init_db(con)
    con.execute(
        "INSERT INTO items (id, type, title, author, platform, source_url, "
        "published_at, collected_at, summary, keywords, content_hash, synced_at) "
        "VALUES ('x','blog','标题','作者','p','https://x/x','2024-01-01',"
        "'2024-01-02','s','[]','h','2024-01-02')")
    con.execute("UPDATE items SET cluster = 5")    # stale assignment to be cleared
    need = cluster.recompute_clusters(con)
    assert need == {}
    assert con.execute("SELECT cluster FROM items WHERE id='x'").fetchone()["cluster"] is None
    assert con.execute("SELECT count(*) FROM clusters").fetchone()[0] == 0
    con.close()


def test_recompute_carries_name_for_unchanged_membership(tmp_path):
    con = schema.connect(tmp_path / "atlas.db")
    schema.init_db(con)
    for rid, vec in [("a", [1.0, 0.0]), ("b", [0.98, 0.1]),
                     ("c", [-1.0, 0.0]), ("d", [-0.98, 0.1])]:
        _seed(con, rid, vec)
    cluster.recompute_clusters(con, seed=7)
    con.execute("UPDATE clusters SET name = '已命名'")     # simulate naming step
    need = cluster.recompute_clusters(con, seed=7)         # identical data -> same signatures
    assert need == {}                                      # nothing needs (re)naming
    assert all(r["name"] == "已命名" for r in con.execute("SELECT name FROM clusters"))
    con.close()
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/unit/test_db_cluster.py -k recompute -v`
Expected: FAIL。

- [ ] **Step 3: 实现 `recompute_clusters`**(追加到 `cluster.py`)

```python
def _signature(ids: list[str]) -> str:
    return hashlib.sha1("\x00".join(sorted(ids)).encode("utf-8")).hexdigest()


def recompute_clusters(con, *, seed: int = CLUSTER_SEED) -> dict[int, list[str]]:
    """Recompute global clusters from stored embeddings; rewrite items.cluster and
    the clusters table. A cluster's name is carried over when its exact member set
    (signature) is unchanged, so only genuinely new/changed clusters need (LLM)
    naming. Returns {cluster_id: [representative titles]} for clusters that still
    need a name — empty when there are no embeddings."""
    groups = graph._load_groups(con)
    prev_names = {
        r["signature"]: r["name"]
        for r in con.execute("SELECT signature, name FROM clusters")
        if r["signature"] and r["name"]
    }
    con.execute("UPDATE items SET cluster = NULL")
    con.execute("DELETE FROM clusters")
    if not groups:
        return {}
    dim = max(groups, key=lambda d: len(groups[d][0]))
    ids, mat = groups[dim]
    labels = kmeans(mat, choose_k(len(ids)), seed=seed)
    reps = representatives(labels, mat, ids)

    members: dict[int, list[str]] = {}
    for sid, lab in zip(ids, labels):
        con.execute("UPDATE items SET cluster = ? WHERE id = ?", (int(lab), sid))
        members.setdefault(int(lab), []).append(sid)

    title_of = {r["id"]: r["title"]
                for r in con.execute("SELECT id, title FROM items WHERE cluster IS NOT NULL")}
    need: dict[int, list[str]] = {}
    for cid in sorted(members):
        sig = _signature(members[cid])
        name = prev_names.get(sig)                 # carry name when membership unchanged
        con.execute(
            "INSERT INTO clusters (id, name, color, signature) VALUES (?,?,?,?)",
            (cid, name, color_for(cid), sig),
        )
        if not name:
            need[cid] = [title_of[i] for i in reps.get(cid, []) if i in title_of]
    return need
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest tests/unit/test_db_cluster.py -v`
Expected: PASS（全文件）。

- [ ] **Step 5: 提交**

```bash
git add src/aishelf/db/cluster.py tests/unit/test_db_cluster.py
git commit -m "feat(db): recompute_clusters（写 items.cluster + clusters 表，签名沿用名字）"
```

---

## Task 6: `alias.generate_cluster_names`

**Files:**
- Modify: `src/aishelf/alias.py`
- Test: `tests/unit/test_alias.py`

- [ ] **Step 1: 写失败测试**(追加到 `tests/unit/test_alias.py` 末尾)

```python
def test_generate_cluster_names_not_configured_returns_none(monkeypatch):
    monkeypatch.delenv("ATLAS_CHAT_BASE_URL", raising=False)
    monkeypatch.delenv("ATLAS_CHAT_MODEL", raising=False)
    assert alias.generate_cluster_names([["t1", "t2"]]) is None


def test_generate_cluster_names_parses_json_array(monkeypatch):
    _configure(monkeypatch)
    monkeypatch.setattr(alias, "get_client", lambda s: _FakeClient('["智能体", "检索增强"]'))
    out = alias.generate_cluster_names([["Agent 入门", "多智能体"], ["RAG 综述", "向量检索"]])
    assert out == ["智能体", "检索增强"]


def test_generate_cluster_names_empty_returns_none(monkeypatch):
    _configure(monkeypatch)
    assert alias.generate_cluster_names([]) is None


def test_generate_cluster_names_wrong_length_returns_none(monkeypatch):
    _configure(monkeypatch)
    monkeypatch.setattr(alias, "get_client", lambda s: _FakeClient('["仅一个"]'))
    assert alias.generate_cluster_names([["a"], ["b"]]) is None


def test_generate_cluster_names_caps_length(monkeypatch):
    _configure(monkeypatch)
    monkeypatch.setattr(alias, "get_client", lambda s: _FakeClient('["一二三四五六七八九十"]'))
    out = alias.generate_cluster_names([["a"]])
    assert len(out) == 1 and len(out[0]) <= alias.CLUSTER_NAME_MAX_CHARS
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/unit/test_alias.py -k cluster_names -v`
Expected: FAIL（`module 'aishelf.alias' has no attribute 'generate_cluster_names'`）。

- [ ] **Step 3: 实现 `generate_cluster_names`**(在 `alias.py` 的 `generate_hooks` 之后追加,并在 `_HOOK_PROMPT` 附近加常量)

```python
CLUSTER_NAME_MAX_CHARS = 8  # safety cap; the prompt asks for <= 6 Chinese chars

_CLUSTER_PROMPT = (
    "下面每一行是一个主题簇下的若干内容标题。请为每个簇起一个不超过6个汉字的"
    "中文主题名，要能概括该簇的共同主题、尽量短。只返回一个 JSON 字符串数组，"
    "元素顺序与输入编号一致，不要输出任何额外文字或解释。\n\n"
)
```

```python
def generate_cluster_names(clusters: list[list[str]]) -> list[str] | None:
    """≤6-char Chinese theme names for clusters, each given as its representative
    titles. Reuses the batched core; None when unconfigured/empty/error/mismatch."""
    pairs = [("、".join(titles[:6]), "") for titles in clusters]
    return _generate_strings(_CLUSTER_PROMPT, pairs, CLUSTER_NAME_MAX_CHARS)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest tests/unit/test_alias.py -v`
Expected: PASS（全文件）。

- [ ] **Step 5: 提交**

```bash
git add src/aishelf/alias.py tests/unit/test_alias.py
git commit -m "feat: alias.generate_cluster_names（星系中文主题名）"
```

---

## Task 7: 把聚类 + 命名接进 `sync()`

**Files:**
- Modify: `src/aishelf/db/sync.py`
- Test: `tests/unit/test_db_sync.py`

- [ ] **Step 1: 写失败测试**(追加到 `tests/unit/test_db_sync.py` 末尾；复用文件里已有的 `_write_video`)

```python
# ---------------------------------------------------------------------------
# Cluster tests (主题星系)
# ---------------------------------------------------------------------------

def _cluster_of(db_path, vid):
    con = schema.connect(db_path)
    row = con.execute("SELECT cluster FROM items WHERE id=?", (vid,)).fetchone()
    con.close()
    return row["cluster"]


def test_sync_clusters_items_when_embeddings_configured(tmp_path, monkeypatch):
    data, db = tmp_path / "data", tmp_path / "atlas.db"
    _write_video(data, "v1")
    _write_video(data, "v2")
    monkeypatch.setattr(embed, "model_name", lambda: "fake-embed")
    monkeypatch.setattr(embed, "embed_texts", lambda texts: [[1.0, 0.0], [0.97, 0.2]][: len(texts)])
    monkeypatch.setattr(_alias, "is_configured", lambda: False)   # isolate clustering from naming
    sync(data, db)
    assert _cluster_of(db, "v1") is not None
    con = schema.connect(db)
    assert con.execute("SELECT count(*) FROM clusters").fetchone()[0] >= 1
    con.close()


def test_sync_no_clusters_when_embeddings_unconfigured(tmp_path, monkeypatch):
    data, db = tmp_path / "data", tmp_path / "atlas.db"
    _write_video(data, "v1")
    monkeypatch.setattr(embed, "model_name", lambda: None)
    monkeypatch.setattr(_alias, "is_configured", lambda: False)
    sync(data, db)
    assert _cluster_of(db, "v1") is None
    con = schema.connect(db)
    assert con.execute("SELECT count(*) FROM clusters").fetchone()[0] == 0
    con.close()


def test_sync_names_clusters_when_alias_configured(tmp_path, monkeypatch):
    data, db = tmp_path / "data", tmp_path / "atlas.db"
    _write_video(data, "v1")
    _write_video(data, "v2")
    monkeypatch.setattr(embed, "model_name", lambda: "fake-embed")
    monkeypatch.setattr(embed, "embed_texts", lambda texts: [[1.0, 0.0], [0.97, 0.2]][: len(texts)])
    monkeypatch.setattr(_alias, "is_configured", lambda: True)
    monkeypatch.setattr(_alias, "generate_aliases", lambda pairs: None)
    monkeypatch.setattr(_alias, "generate_hooks", lambda pairs: None)
    monkeypatch.setattr(_alias, "generate_cluster_names", lambda clusters: ["星系名"] * len(clusters))
    sync(data, db)
    con = schema.connect(db)
    names = [r["name"] for r in con.execute("SELECT name FROM clusters")]
    con.close()
    assert names and all(n == "星系名" for n in names)


def test_sync_note_edit_does_not_rename_clusters(tmp_path, monkeypatch):
    data, db = tmp_path / "data", tmp_path / "atlas.db"
    _write_video(data, "v1")
    _write_video(data, "v2")
    monkeypatch.setattr(embed, "model_name", lambda: "fake-embed")
    monkeypatch.setattr(embed, "embed_texts", lambda texts: [[1.0, 0.0], [0.97, 0.2]][: len(texts)])
    monkeypatch.setattr(_alias, "is_configured", lambda: True)
    monkeypatch.setattr(_alias, "generate_aliases", lambda pairs: None)
    monkeypatch.setattr(_alias, "generate_hooks", lambda pairs: None)
    calls = {"n": 0}

    def _name(clusters):
        calls["n"] += 1
        return ["星系名"] * len(clusters)

    monkeypatch.setattr(_alias, "generate_cluster_names", _name)
    sync(data, db)
    assert calls["n"] == 1
    # note-only edit: re-embeds + re-clusters, but membership (signature) is
    # unchanged, so the LLM naming call must NOT fire again.
    _write_note(data, "v1", "新增一段笔记")
    sync(data, db)
    assert calls["n"] == 1
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/unit/test_db_sync.py -k cluster -v`
Expected: FAIL（`clusters` 始终为空 / cluster 列为 None）。

- [ ] **Step 3: 改 `sync.py`**

把模块导入行改为引入 `cluster`：

```python
from aishelf.db import schema, tokenize, vector, graph, cluster
```

在 `sync()` 里、`if embedded_ids: graph.recompute_edges_for(...)` 这段**之后**、`con.commit()` **之前**插入：

```python
        # Knowledge-graph clusters (主题星系): recompute when the embedding set
        # changed; (LLM-)name only clusters whose membership is new.
        if embedded_ids or stale:
            to_name = cluster.recompute_clusters(con)
            if to_name and alias.is_configured():
                ordered = sorted(to_name)
                names = alias.generate_cluster_names([to_name[c] for c in ordered])
                if names and len(names) == len(ordered):
                    for cid, nm in zip(ordered, names):
                        con.execute("UPDATE clusters SET name=? WHERE id=?", (nm, cid))
                elif names:
                    logger.warning(
                        "generate_cluster_names returned %d for %d clusters; skipping",
                        len(names), len(ordered),
                    )
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest tests/unit/test_db_sync.py -v`
Expected: PASS（全文件，含既有 embedding/alias/hook/edge 用例）。

- [ ] **Step 5: 提交**

```bash
git add src/aishelf/db/sync.py tests/unit/test_db_sync.py
git commit -m "feat(db): sync 时重算主题星系 + 按需命名（改笔记不重命名）"
```

---

## Task 8: `load_graph` 输出 `cluster`/`collected_at`/`hook` + 顶层 `clusters`

**Files:**
- Modify: `src/aishelf/db/graph.py`
- Test: `tests/unit/test_db_graph.py`

- [ ] **Step 1: 写失败测试**(追加到 `tests/unit/test_db_graph.py` 末尾)

```python
def test_load_graph_nodes_carry_cluster_collected_at_hook(tmp_path):
    db = tmp_path / "atlas.db"
    con = schema.connect(db)
    schema.init_db(con)
    _seed_item(con, "a", "video", [1.0, 0.0])
    con.execute("UPDATE items SET cluster=0, hook='值得一看' WHERE id='a'")
    con.commit(); con.close()
    g = graph.load_graph(db)
    a = next(n for n in g["nodes"] if n["id"] == "a")
    assert a["cluster"] == 0
    assert a["collected_at"] == "2024-01-02"     # from _seed_item
    assert a["hook"] == "值得一看"


def test_load_graph_emits_clusters_list(tmp_path):
    db = tmp_path / "atlas.db"
    con = schema.connect(db)
    schema.init_db(con)
    _seed_item(con, "a", "video", [1.0, 0.0])
    _seed_item(con, "b", "blog", [0.97, 0.2])
    con.execute("UPDATE items SET cluster=0 WHERE id IN ('a','b')")
    con.execute("INSERT INTO clusters (id, name, color, signature) VALUES (0,'智能体','#ff9a3c','sig')")
    con.commit(); con.close()
    g = graph.load_graph(db)
    assert g["clusters"] == [{"id": 0, "name": "智能体", "color": "#ff9a3c", "size": 2}]


def test_load_graph_empty_has_clusters_key(tmp_path):
    db = tmp_path / "atlas.db"
    con = schema.connect(db); schema.init_db(con); con.close()
    assert graph.load_graph(db) == {"nodes": [], "edges": [], "clusters": []}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/unit/test_db_graph.py -k "carry_cluster or clusters_list or clusters_key" -v`
Expected: FAIL（节点无 `cluster` 键 / 无 `clusters` 顶层键）。

- [ ] **Step 3: 改 `load_graph`**

把读取 `node_rows` 的 SELECT 扩展为带上新列,并在其后读取 `clusters` 表（在 `groups = _load_groups(con)` 之前）：

```python
        node_rows = con.execute(
            "SELECT id, type, title, alias, cluster, collected_at, hook FROM items"
        ).fetchall()
        edge_rows = con.execute("SELECT src, dst, weight FROM edges").fetchall()
        cluster_rows = con.execute("SELECT id, name, color FROM clusters").fetchall()
        groups = _load_groups(con)
```

在构建 `nodes` 之前算每簇成员数,并构造 `clusters`（放在 `positions = _sphere_positions(...)` 之前）：

```python
    csize: dict[int, int] = {}
    for r in node_rows:
        if r["cluster"] is not None:
            csize[r["cluster"]] = csize.get(r["cluster"], 0) + 1
    clusters = [
        {"id": r["id"], "name": r["name"] or "", "color": r["color"] or "",
         "size": csize.get(r["id"], 0)}
        for r in cluster_rows
    ]
```

把每个 node dict 扩展为带上新字段：

```python
        nodes.append({
            "id": r["id"], "type": r["type"], "title": r["title"],
            "alias": r["alias"] or "", "degree": degree.get(r["id"], 0),
            "cluster": r["cluster"], "collected_at": r["collected_at"],
            "hook": r["hook"] or "",
            "x": x, "y": y, "z": z,
        })
    return {"nodes": nodes, "edges": edges, "clusters": clusters}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest tests/unit/test_db_graph.py -v`
Expected: PASS（全文件；注意既有的 `test_load_graph_empty` 已被本任务的 `test_load_graph_empty_has_clusters_key` 取代——若旧用例仍断言无 `clusters` 键，将其更新为带 `"clusters": []`）。

- [ ] **Step 5: 更新旧的 `test_load_graph_empty`**

把既有用例改为：

```python
def test_load_graph_empty(tmp_path):
    db = tmp_path / "atlas.db"
    con = schema.connect(db); schema.init_db(con); con.close()
    assert graph.load_graph(db) == {"nodes": [], "edges": [], "clusters": []}
```

Run: `pytest tests/unit/test_db_graph.py -v`
Expected: PASS。

- [ ] **Step 6: 提交**

```bash
git add src/aishelf/db/graph.py tests/unit/test_db_graph.py
git commit -m "feat(db): load_graph 输出簇/采集时间/钩子 + 顶层 clusters"
```

---

## Task 9: 路由烟雾测试 — `/api/graph` 含 `clusters`

**Files:**
- Test: `tests/unit/test_site_graph_routes.py`

> 该文件已有一个 `client` fixture（`monkeypatch` 设 `AISHELF_DATA_DIR`/`AISHELF_DB_PATH`
> 到 `tmp_path`、写一条视频、`sync` 后返回 `TestClient(app)`；测试环境无 `ATLAS_EMBED_*`，
> 所以无 embedding、无边、无簇）。下面两处都复用它。

- [ ] **Step 1: 更新既有 `test_api_graph_shape`（Task 8 已让载荷新增 `clusters` 键，旧断言会失败）**

把第 27 行的断言改为：

```python
    assert set(body.keys()) == {"nodes", "edges", "clusters"}
```

并在该用例末尾追加一行（无 embedding 时簇为空）：

```python
    assert body["clusters"] == []
```

- [ ] **Step 2: 追加一个显式用例**

在文件末尾追加：

```python
def test_api_graph_payload_has_clusters_list(client):
    body = client.get("/api/graph").json()
    assert "clusters" in body and isinstance(body["clusters"], list)
```

- [ ] **Step 3: 跑测试确认通过**

Run: `pytest tests/unit/test_site_graph_routes.py -v`
Expected: PASS（含更新后的 `test_api_graph_shape`）。

- [ ] **Step 4: 提交**

```bash
git add tests/unit/test_site_graph_routes.py
git commit -m "test(site): /api/graph 载荷含 clusters 键"
```

---

## Task 10: 前端 — 按簇上色 + 星系标签 + 图例 + 动词药壳（手动验证）

**Files:**
- Modify: `src/aishelf/site/templates/graph.html`

> 本任务无自动化测试(与现有 `graph.html` 纯手验 JS 一致)。每步都是对现有文件的定向编辑;改完用真实站点肉眼验收。

- [ ] **Step 1: CSS — 星系标签、图例、动词药丸**

在 `<style>` 块内、`.globe-empty code{...}` 之后追加：

```css
  .galaxy-label { color: #eaf2ff; font-size: 14px; font-weight: 700; opacity: .55;
                  text-shadow: 0 0 10px rgba(40,120,220,.8); pointer-events: none; white-space: nowrap; }
  .globe-legend { display: flex; align-items: center; gap: 4px; flex-wrap: wrap; }
  .globe-legend button { display: inline-flex; align-items: center; gap: 5px; height: 28px;
                         padding: 0 10px; border: 1px solid #1d2a48; background: #0c1426;
                         color: #aebbd9; border-radius: 999px; cursor: pointer; font-size: 12px; }
  .globe-legend button.off { opacity: .4; }
  .globe-legend .sw { width: 9px; height: 9px; border-radius: 50%; display: inline-block; }
  .verb-pill { position: absolute; left: 50%; bottom: 16px; transform: translateX(-50%);
               display: flex; gap: 6px; background: rgba(10,15,30,.85); border: 1px solid #24365f;
               border-radius: 999px; padding: 5px 7px; z-index: 3; }
  .verb-pill button { border: none; background: transparent; color: #bcd4ff; font-size: 13px;
                      padding: 4px 12px; border-radius: 999px; cursor: not-allowed; opacity: .5; }
```

- [ ] **Step 2: HTML — 图例容器 + 动词药丸**

把 `.globe-bar` 里的 `.globe-filters`（视频/博客那组）保留，并在其后追加一个图例容器：

```html
      <div class="globe-legend" id="globeLegend"></div>
```

在 `<div id="globe" class="globe-canvas"></div>` 之后、`#globeTip` 之前加动词药丸：

```html
  <div class="verb-pill" id="verbPill">
    <button type="button" disabled title="即将上线">▶ 回放</button>
    <button type="button" disabled title="即将上线">🛸 逛逛</button>
    <button type="button" disabled title="即将上线">🔗 路径</button>
  </div>
```

- [ ] **Step 3: JS — 按簇上色**

把节点配色从类型二元色改为优先按簇。先在 `const COLOR = {...}` 之后建簇色查表：

```javascript
  const COLOR = { video: 0xff9a3c, blog: 0x4cc2ff };
  const CLUSTER_COLOR = {};
  for (const c of (data.clusters || [])) {
    if (c.color) CLUSTER_COLOR[c.id] = parseInt(c.color.replace('#', '0x'));
  }
  const nodeColor = (n) =>
    (n.cluster != null && CLUSTER_COLOR[n.cluster] != null) ? CLUSTER_COLOR[n.cluster]
      : (COLOR[n.type] || 0x9aa6bb);
```

在建 sprite 的循环里,把 `color: COLOR[n.type] || 0x9aa6bb` 改为 `color: nodeColor(n)`。

为保留"类型可辨",给博客节点加一个细描边圈:在该循环内、`scene.add(sprite); nodeMeshes.push(sprite);` 之前追加：

```javascript
    if (n.type === 'blog') {
      const ring = new THREE.Sprite(new THREE.SpriteMaterial({
        map: DOT, color: 0xffffff, transparent: true, opacity: 0.25, depthWrite: false,
      }));
      ring.scale.set(size * 1.7, size * 1.7, 1);
      ring.position.copy(sprite.position);
      scene.add(ring);
    }
```

- [ ] **Step 4: JS — 漂浮星系标签**

在节点循环**之后**追加：按簇聚合成员世界坐标求质心,放一个 `.galaxy-label`：

```javascript
  const galaxyAcc = {};   // cluster id -> {name, sx, sy, sz, n}
  for (const n of data.nodes) {
    if (n.cluster == null) continue;
    const g = galaxyAcc[n.cluster] || (galaxyAcc[n.cluster] = { sx: 0, sy: 0, sz: 0, n: 0 });
    g.sx += n.x; g.sy += n.y; g.sz += n.z; g.n++;
  }
  const clusterName = {};
  for (const c of (data.clusters || [])) clusterName[c.id] = c.name || '';
  for (const [cid, g] of Object.entries(galaxyAcc)) {
    const name = clusterName[cid] || ('星系 ' + cid);
    const cx = g.sx / g.n, cy = g.sy / g.n, cz = g.sz / g.n;
    const div = document.createElement('div');
    div.className = 'galaxy-label';
    div.textContent = name;
    const label = new THREE.CSS2DObject(div);
    label.position.set(cx * R * 1.08, cy * R * 1.08, cz * R * 1.08);
    scene.add(label);
  }
```

- [ ] **Step 5: JS — 图例(簇色块筛选)**

在现有 `globeFilters` 的事件绑定附近,渲染图例并接筛选(点击某星系 → 只显示该簇节点,再点恢复全部)：

```javascript
  (function buildLegend() {
    const box = document.getElementById('globeLegend');
    let active = null;
    for (const c of (data.clusters || [])) {
      const b = document.createElement('button');
      b.innerHTML = '<span class="sw" style="background:' + (c.color || '#888') + '"></span>'
        + (c.name || ('星系 ' + c.id));
      b.addEventListener('click', () => {
        active = (active === c.id) ? null : c.id;
        document.querySelectorAll('#globeLegend button').forEach((x) => x.classList.remove('off'));
        for (const m of nodeMeshes) {
          const vis = (active == null) || (m.userData.cluster === active);
          m.material.opacity = vis ? 1 : 0.08;
          if (m.userData._label) m.userData._label.element.style.opacity = vis ? '1' : '0.08';
        }
        if (active != null) {
          document.querySelectorAll('#globeLegend button').forEach((x, i) => {
            if ((data.clusters[i] || {}).id !== active) x.classList.add('off');
          });
        }
      });
      box.appendChild(b);
    }
  })();
```

- [ ] **Step 6: 手动验证(配置好 embedding + chat 的环境)**

```bash
python -m aishelf.db sync --rebuild
python -m aishelf.site
```

打开 `http://127.0.0.1:8001/graph`,逐项确认:
1. 节点按星系上色(不再是单纯橙/蓝);博客节点有一圈淡描边。
2. 球面上漂浮着若干中文星系名(无名时回退"星系 N")。
3. 顶栏出现图例色块;点某星系 → 只高亮该簇,再点恢复。
4. 视频/博客类型筛选仍工作。
5. 底部出现 `▶ 回放 · 🛸 逛逛 · 🔗 路径` 药丸(禁用态,hover 显示"即将上线")。
6. 悬停 tooltip、点击进详情、拖拽旋转、滚轮缩放、自动旋转一切照旧。

> 若 embedding 未配置,`/api/graph` 的 `clusters` 为空 → 页面回退到原有类型配色、无星系标签、图例为空,不报错。也确认这一降级路径。

- [ ] **Step 7: 提交**

```bash
git add src/aishelf/site/templates/graph.html
git commit -m "feat(graph): 主题星系前端（按簇上色 + 星系标签 + 图例 + 动词药壳）"
```

---

## Task 11: 文档同步（CLAUDE.md）

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: 更新 db/ 段落**

在 `aishelf/db/` 的描述中加入 `cluster.py`(纯 `choose_k`/`kmeans`/`representatives`/`color_for` + 薄 `recompute_clusters`;sync 时按 `embedded_ids or stale` 重算、按成员签名沿用名字),并说明 `schema.py` 现含 `clusters` 表(`id,name,color,signature`)与 `items.cluster` 列;`sync` 在边重算后重算星系并经 `alias.generate_cluster_names` 命名(改笔记不重命名);`load_graph` 现额外输出节点的 `cluster/collected_at/hook` 与顶层 `clusters`。

- [ ] **Step 2: 更新 alias.py 段落**

补充 `generate_cluster_names(clusters)` —— 同 `ATLAS_CHAT_*` 门、同批量内核、同 None 降级,返回每簇 ≤6 字中文主题名。

- [ ] **Step 3: 更新部署说明**

在讲 `sync --rebuild` 回填的那段补上"回填 `cluster` 列与 `clusters` 表(主题星系)"。

- [ ] **Step 4: 提交**

```bash
git add CLAUDE.md
git commit -m "docs: 图谱主题星系（cluster.py / clusters 表 / load_graph 富化）"
```

---

## Final Verification

- [ ] 全量测试(默认跳过 network):

Run: `pytest`
Expected: 全绿。

- [ ] 受影响子集快速复跑:

Run: `pytest tests/unit/test_db_cluster.py tests/unit/test_db_sync.py tests/unit/test_db_graph.py tests/unit/test_alias.py tests/unit/test_site_graph_routes.py -v`
Expected: 全绿。

- [ ] 手动:`python -m aishelf.db sync --rebuild` 后 `/graph` 按 Task 10 Step 6 的清单逐项过一遍(含 embedding 未配置的降级路径)。

---

## Self-Review（已核对）

- **Spec 覆盖**:数据底座(`clusters` 表 + `items.cluster` + `load_graph` 富化)→ Task 1/8;聚类 → Task 2–5;命名 → Task 6;接入 sync(含改笔记不重命名)→ Task 7;前端上色/星系名/图例/动词壳 → Task 10;部署 `--rebuild` 回填 → Task 1 Step 4 + Task 11。B/D/E 的动词逻辑明确留给 Phase 2(本计划只放禁用态药壳)。
- **占位符**:无 TBD/TODO;每个代码步骤均给出完整代码。
- **类型/命名一致**:`recompute_clusters` 返回 `{int: [titles]}` 在 Task 5 定义、Task 7 消费一致;`generate_cluster_names(list[list[str]]) -> list[str] | None` 在 Task 6 定义、Task 7 调用一致;`clusters` 表四列 `id/name/color/signature` 在 Task 1/5/8 全程一致;`load_graph` 顶层键 `nodes/edges/clusters` 在 Task 8/9 一致。
- **已知边界**:前端 Three.js 交互无自动化测试(与现状一致,靠 Task 10 手验);新列依赖 `sync --rebuild` 回填(沿用 embedding/alias/hook 的既有约定,无在线迁移)。
